# Genesis Core Dependencies — Python 3.11+
# Install with: pip install -r requirements.genesis.txt

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async & Task Queue
celery>=5.3.0
redis>=5.0.0
aiohttp>=3.9.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# GitHub API
PyGithub>=2.1.0
githubkit>=0.11.0

# Vector Store — optional backends (install as needed)
# chromadb>=0.4.22       # ChromaDB persistent vector store
# numpy>=1.26.0          # Dense vector math (required for OpenAI/Ollama embeddings)

# LLM SDKs — optional; BaseAgent uses raw urllib if these are absent
# openai>=1.12.0         # OpenAI Python SDK
# groq>=0.4.0            # Groq Python SDK
# google-generativeai>=0.5.0  # Google Gemini Python SDK

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Code Quality
black>=23.11.0
pylint>=3.0.0
mypy>=1.7.0
isort>=5.12.0

# Development
python-dotenv>=1.0.0
